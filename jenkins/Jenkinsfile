pipeline {
    agent any
    tools{
       maven 'maven123'
         }
       environment {
        IMAGE_NAME = "ecommerce-app"           // ✅ Your DockerHub repo name
        DOCKER_CREDENTIALS_ID = "ae01a98c-daad-47ab-be7b-500b88edd30b"  // ✅ From your Jenkins credenti        GIT_CREDENTIALS_ID = "04ce9bce-f651-4f5f-9582-1fe650bd5127"     // ✅ Your GitHub credentials
    }

    stages {
        stage('Clone Code') {
            steps {
                git branch: 'main', url: 'https://github.com/Git-Srinivas5/ecommerce-devops-project.git'
            }
        }

        stage('Build with Maven') {
            steps {
                dir('backend') {
                    sh 'mvn clean package -DskipTests'
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                script {
                    docker.build("${IMAGE_NAME}", "backend")
                }
            }
        }

        // Optional stage: push to DockerHub or JFrog
        /*
        stage('Push to DockerHub') {
            steps {
                script {
                    docker.withRegistry('', 'docker-hub-credentials-id') {
                        docker.image("${IMAGE_NAME}").push('latest')
                    }
                }
            }
        }
        */
    }

    post {
        success {
            echo '✅ Build and Docker image successful!'
        }
        failure {
            echo '❌ Build failed. Check logs.'
        }
    }
}

